<manifest 
  xmlns:android="http://schemas.android.com/apk/res/android"
  package="karuta.hpnpwd.share_screen_shot" android:installLocation="auto"
  android:versionName="0.0.1" android:versionCode="1" >
    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="16" ></uses-sdk>
    <application android:icon="@android:drawable/star_big_on" android:label="@string/app_name" >
        <activity android:launchMode="standard" android:label="@string/app_name" android:name=".MainActivity" android:theme="@style/Theme.Transparent" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
    </application>
</manifest>
